{
  "name": "conference-feedback",
  "version": "0.1.0",
  "private": true,
  "proxy": "https://akhenr2oenza574lweyxuo2c240smjjd.lambda-url.eu-west-1.on.aws",
  "dependencies": {
    "bootstrap": "^5.3.6",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-rating-stars-component": "^2.2.0",
    "react-router-dom": "^6.0.0",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "cdk:deploy": "cd my-static-site && cdk deploy",
    "cdk:destroy": "cd my-static-site && cdk destroy",
    "deploy": "npm run build && aws s3 cp build s3://conference-feedback-static-site-holisticon --recursive"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
